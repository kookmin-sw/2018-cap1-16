# 정적 이진 Check Point 위치
STATIC_BC_CHECK_POINT = "정적 이진 분류 모델 체크포인트 위치"

# 정적 멀티 Check Point 위치
STATIC_MC_CHECK_POINT = "정적 다중 분류 모델 체크포인트 위치"

# 정적 분석 학습 파일 위치
TRAIN_STATIC_DATA_PATH = "정적 피쳐 벡터(fhfops)가 저장되어 있는 경로"

# 동적 이진 Check Point 위치
DYNAMIC_BC_CHECK_POINT = "동적 이진 분류 모델 체크포인트 위치"

# 동적 멀티 Check Point 위치
DYNAMIC_MC_CHECK_POINT = "동적 이진 분류 모델 체크포인트 위치"

# 동적 분석 학습 파일 위치
TRAIN_DYNAMIC_DATA_PATH = "동적 피쳐 벡터(fhacs)가 저장되어 있는 경로"

# 학습에 사용할 라벨
TRAIN_LABEL_PATH = "학습에 사용될 csv(md5, detected, label)이 저장되어 있는 경로"

